<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/launcher_vertical_padding"
        android:paddingLeft="@dimen/launcher_horizontal_padding"
        android:paddingRight="@dimen/launcher_horizontal_padding"
        android:paddingTop="@dimen/launcher_vertical_padding"
        tools:context="ng.com.tinweb.www.simone20.WelcomeActivity">

        <TextView
            android:id="@+id/app_title"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAllCaps="true"
            android:textSize="40sp"
            android:text="@string/app_name"
            app:font="@{`AppName`}"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/app_title"
            android:layout_centerHorizontal="true"
            android:fontFamily="sans-serif-light"
            android:textSize="16sp"
            android:layout_marginTop="5dp"
            android:text="@string/app_slogan"/>

        <ImageView
            android:id="@+id/logo"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/icon_blue"
            android:layout_centerInParent="true"
            android:contentDescription="@string/logo_description" />

        <Button
            android:layout_alignParentBottom="true"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:background="@drawable/launcher_button"
            android:layout_centerHorizontal="true"
            android:text="@string/get_started"/>
    </RelativeLayout>

</layout>
